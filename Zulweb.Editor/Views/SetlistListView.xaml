<UserControl x:Class="Zulweb.Editor.Views.SetlistListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             xmlns:views="clr-namespace:Zulweb.Editor.Views"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance views:SetlistList}"
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel>

        <UniformGrid Columns="2" DockPanel.Dock="Bottom">
            <Button Command="{Binding Path=AddCommand}">Add</Button>
            <Button Command="{Binding Path=DeleteCommand}">Remove</Button>
        </UniformGrid>

        <dxg:GridControl ItemsSource="{Binding Path=Items}" SelectedItem="{Binding Path=SelectedItem}">
            <dxg:GridControl.Columns>
                <dxg:GridColumn FieldName="Name" Width="*" />
            </dxg:GridControl.Columns>
            <dxg:GridControl.View>
                <dxg:TableView AllowGrouping="False" />
            </dxg:GridControl.View>
        </dxg:GridControl>

    </DockPanel>
</UserControl>